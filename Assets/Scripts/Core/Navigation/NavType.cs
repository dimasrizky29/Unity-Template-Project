public enum NavType
{
    ScenePage,      // Panel biasa yang menempel di Scene (contoh: Main Menu, HUD)
    Window,         // Floating Fullscreen (contoh: Shop, Inventory) - via Addressables
    Popup           // Floating Small (contoh: Confirmation) - via Addressables
}
